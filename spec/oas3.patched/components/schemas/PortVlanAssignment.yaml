properties:
  created_at:
    format: date-time
    type: string
    readOnly: true
  id:
    format: uuid
    type: string
    readOnly: true
  native:
    type: boolean
  port:
    $ref: './Href.yaml'
  state:
    enum:
    - assigned
    - unassigning
    type: string
  updated_at:
    format: date-time
    type: string
    readOnly: true
  virtual_network:
    $ref: './Href.yaml'
  vlan:
    type: integer
type: object
